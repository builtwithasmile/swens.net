# Current State — live status only. Facts, not canon. Never "decided / locked".

## Open loops (carry-forward — surfaced at session start, never blocks a close)
- ~~Validator missing maxLen()/pattern() (Selvatec security defaults gap)~~ — **RESOLVED 2026-07-08:** added `maxLen()` and `pattern()` to `core/Validator.php`, matching the factory template's implementation (array-input guards included). No callers yet — controllers still do inline length/format checks; wiring them to use Validator is a future pass.
- ~~Raw mail() calls (Selvatec security defaults gap)~~ — **RESOLVED 2026-07-08:** added CRLF-hardened `send_mail()` wrapper in `core/helpers.php`; `controllers/admin/AuthController.php` and `controllers/web/GateController.php` now call it instead of raw `@mail()`.
- ~~No git remote configured~~ — **RESOLVED 2026-07-07:** remote added (github.com/builtwithasmile/swens.net) and `main` pushed (`2412504`).
- **Live deploy — its own repo session (Josh 2026-07-07, deferred):** the main docroot `/home/swensnet/public_html` is SHARED with live apps `capp/` (capp.swens.net — BC cannabis compliance) and `me/` (me.swens.net), so the deploy MUST rsync-exclude them or it clobbers them. `.cpanel.yml` is a deliberate disabled placeholder; going live needs the real exclude-preserving tasks + a root-rewrite written first (the app wants `/public` as docroot, which a main domain can't point at — docs/architecture.md §10, not yet written), then verify capp.swens.net + me.swens.net still serve before AND after. Do NOT blind-deploy.
- DIRECTION.md still Josh's to write (Porch pivot).
- ~~Stale "Compound" reference in config.example.php~~ — **RESOLVED 2026-07-07:** header comment updated to Porch. `services/Buildings.php`'s Compound mention is accurate historical context (describes the retired six-place metaphor), not stale — left unchanged.
- ~~Admin post-form kind options mismatched keyedSections() (`InsideController.php` lines 123-129)~~ — **RESOLVED 2026-07-08:** `templates/pages/admin/post-form.php` line 33 dropdown changed from `['note','build','photo','update','product']` to `['welcome','about','board','now','story']`; default fallback updated `'note'` → `'welcome'` to match.
- ~~`controllers/admin/PostsController.php:133` defaulted `kind` to `'note'`~~ — **RESOLVED 2026-07-08:** default changed to `'welcome'`, consistent with the post-form dropdown default and `InsideController::keyedSections()`'s valid kinds (welcome/about/board/now/story).
